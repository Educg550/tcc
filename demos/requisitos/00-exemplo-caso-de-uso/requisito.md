# Requisito NN: <uma linha do que a aplicação faz>

<Um parágrafo: que aplicação é, para quem serve e o que ela substitui. Não repita aqui o
framework nem o ponto de entrada: isso é o `alvo.toml`. Não descreva implementação -
rota, tabela, função, arquivo. Descreva o que o usuário vê e faz.>

## <Tela ou funcionalidade>

Campos, todos obrigatórios, com estes rótulos exatos:

- `RÓTULO EXATO DO CAMPO` (tipo, e a regra de formato quando houver)
- `OUTRO RÓTULO EXATO` (tipo)

Um botão `RÓTULO EXATO DO BOTÃO`.

## Validação

- <situação de erro>: a página volta com a mensagem `MENSAGEM EXATA`.
- <outra situação>: mensagem `OUTRA MENSAGEM EXATA`.

## Após o envio válido

Mostrar a página de confirmação com o título `TÍTULO EXATO` e, abaixo, <a saída>, com os
dados preenchidos no lugar dos marcadores:

```
Campo: <<RÓTULO EXATO DO CAMPO>>
Outro: <<OUTRO RÓTULO EXATO>>
```

Os rótulos e mensagens acima devem estar dispostos exatamente assim.
