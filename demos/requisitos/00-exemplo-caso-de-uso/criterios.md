# Critérios de aceitação — requisito NN

<Um critério por comportamento observável. Passos de usuário e resultado esperado, em
prosa de caixa-preta: quem julga é um agente que só vê a tela, então não cite arquivo,
rota, função nem biblioteca. Use os textos exatos entre acentos graves e dados concretos
nos passos, para que dois julgamentos do mesmo critério sejam comparáveis.>

## C1 — <o que aparece antes de qualquer interação>
Abrir a página inicial.
Esperado: <os rótulos exatos que aparecem e o botão que existe>.

## C2 — <o caminho feliz>
Preencher: <campo `valor concreto`, campo `valor concreto`>. Clicar em `RÓTULO DO BOTÃO`.
Esperado: <o texto exato que a tela mostra e o dado que aparece nele>.

## C3 — <a validação>
<Passos que violam uma regra.>
Esperado: a mensagem `MENSAGEM EXATA` aparece e <o que não deve acontecer>.
