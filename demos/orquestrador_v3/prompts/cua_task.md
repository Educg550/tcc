Você é um usuário testando uma aplicação web de lista de tarefas hospedada em
{base_url}.

Abra {base_url}. Para cada critério abaixo, execute os passos na ordem indicada e
verifique se o `resultado_esperado` realmente acontece na tela. Aja como um
usuário real: clique, digite e observe o resultado.

Para cada critério, decida se passou (o resultado esperado foi observado) ou não,
e registre uma evidência curta do que você viu.

Critérios de aceitação:
{criterios}

Ao final, retorne o veredito estruturado: a lista de critérios com
`passou`/`evidencia`/`obs`, mais `aprovado_geral` (true somente se todos
passaram) e um `resumo` geral.
